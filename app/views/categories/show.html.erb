<div class="container">
<h1><%= @category.name %></h1>

<div class="row">
  <div class="col-md-10">
    <%= will_paginate @posts %>
    <%= render @posts %>
    <%= will_paginate @posts %>
  </div>
  <div class="col-md-2">
    <%= link_to "New Post for #{@category.name}", new_category_post_path(@category), class: 'btn btn-warning pull-right' %>
  </div>
</div>
</div>

