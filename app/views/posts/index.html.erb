<div class="container">
<div class="row">
  <div class="col-md-12">
    <% if logged_in? %>
      <h1>Recent Posts</h1>
      <p class="lead">Active posts in the past 10 days</p>
    <% else %>
      <h1>All Posts</h1>

      <%= will_paginate @posts %>
      <%= render @posts %>
      <%= will_paginate @posts %>
    <% end %>
  </div>
</div>
</div>

